Put this file in your ghostty config directory under the `themes` directory and it will load automatically.

You can then reference the theme in your ghostty config file like so:

```config
theme = "nightfall"
```